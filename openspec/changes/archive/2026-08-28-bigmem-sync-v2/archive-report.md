# Archive Report: bigmem-sync-v2

**Change:** bigmem-sync-v2
**Archived to:** `openspec/changes/archive/2026-08-28-bigmem-sync-v2/`
**Previous location:** `openspec/changes/bigmem-sync-v2/` (active)
**Date:** 2026-08-28 (from verify)
**Status:** PASS (verify 7/7 req 14/14 scen, go vet clean, go test ./internal/bigmem green, tasks 21/21)
**Artifact Store:** both (hybrid) — filesystem `openspec/changes/...` + BigMem `sdd/bigmem-sync-v2/*` topic_key authority
**Commits:** `f3d5904 feat(bigmem): PR1 journal DDL enqueue Ack` / `a39e956 feat(bigmem): PR2 deferred 5->dead` / `8978876 feat(bigmem): PR3 quarantine+lease` + PR4 enrich (CLI status + gates) → 21/21
**Evidence:** `sha256:93076cf2da65e144126303c8ab54e7b19f35a504e35a29b913abcd55dcbbf6f9` (test_output_hash, evidence_revision)

## Summary

Completed bigmem-sync-v2 — ordered sync journal for BigMem. Adds local-only project-scoped journal foundation ported from Engram: `sync_mutations(seq AUTOINCREMENT, project, entity, entity_key, op, payload, source, disposition)`, `sync_state(target_key, project, lifecycle idle|pending|running|healthy|degraded, last_enqueued/acked/pulled_seq, consecutive_failures, backoff_until, lease_owner/until, reason_code)`, `sync_enrolled_projects`, `sync_apply_deferred(attempts, 5→dead hash)` with deterministic dead-letter `sha256(project+entity_key+payload)`, quarantine `quarantined+evidence` + `degraded+reason_code` + cursor advance, lease `Acquire/ReleaseSyncLease` TTL 1m atomic WHERE `lease_until<=now OR lease_owner=owner`, backoff `MarkSyncFailed` exponential `2^failures*2s` capped 1h, and `biggz bigmem sync --status` enrich showing per-target `lifecycle`, `pending`, `last_enqueued/acked/pulled`, `consecutive_failures`, `backoff_until`, `lease_owner/until`. Tables `CREATE TABLE IF NOT EXISTS` coexisting with `sync_chunks`, no DDL on `observations`, no backfill, `enqueueSyncMutationTx` in caller's `BEGIN IMMEDIATE` TX under `Store.mu`.

Shipped as **stacked-to-main 3 PRs + PR4 enrich** (`f3d5904` PR1 DDL+enqueue/Ack, `a39e956` PR2 deferred 5→dead, `8978876` PR3 quarantine+lease, PR4 CLI enrich + `cloud_upgrade_state` skip) covering SYNC-J1/S1/D1/Q1/L1/C1/M1 (7 req, 14 scen), 4 work units.

Verified **PASS** — 14/14 scenarios compliant, **21/21 tasks complete**, `go vet ./...` clean (empty hash `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`), `go test ./internal/bigmem -count=1 -timeout 180s` PASS `ok 5.725s` (12 journal/deferred/quarantine/lease suites green), `biggz bigmem sync --status` shows full lifecycle/pending/last_*/failures/backoff/lease. Delta merged into `openspec/specs/bigmem/spec.md` (now 32 REQ: 8 Engram REQ-1..8 + 8 branching REQ-B1..B8 + 4 ghost REQ-GW1..GW4 + 5 rescue REQ-RO1..RO5 + 7 sync SYNC-J1..M1).

**Final-state handoff (outranks any stale snapshot):** Per orchestrator launch prompt final-state facts, change is **tasks 21/21**, verify **PASS 7/7 req 14/14 scen**, evidence `sha256:93076cf2...` (evidence_revision), PRs `f3d5904/a39e956/8978876` + PR4 enrich, apply-progress `15/21 → 21/21 via PR4 enrich`. Persisted `openspec/changes/archive/2026-08-28-bigmem-sync-v2/tasks.md` is **21/21 [x] 0 [ ]** (filesystem authority), `verify-report.md` is PASS 7/7 14/14 with `evidence_revision sha256:93076cf2da65e144126303c8ab54e7b19f35a504e35a29b913abcd55dcbbf6f9`. Stale intermediate snapshot `apply-progress.md` at archive shows `15/21` (PR3 state) — per Final-State Authority hierarchy, `apply-progress` is intermediate snapshot, valid at its time, but explicit final-state facts `21/21 via PR4 enrich` + persisted `tasks.md` 21/21 outrank it. Stale BigMem `sdd/bigmem-sync-v2/tasks` (`obs-1787964958485057900-1`, len 3772) shows Phase 3-5 `[ ]` ×6 unchecked (snapshot at PR2, 10/21) vs filesystem 21/21 — filesystem wins in hybrid; no stale unchecked tasks remain in archived audit trail. No later commits after `8978876` + PR4 enrich before archive beyond enrich; no contradictions unranked. Per hierarchy, launch prompt final-state facts + persisted tasks artifact outrank stale snapshots; snapshot claims not echoed as current facts.

## Validation

| Check | Result |
|-------|--------|
| Tasks completed | ✅ 21/21 marked [x] — `allComplete: true`, `pending: 0` (`total:21 completed:21` per archived `tasks.md`; Phase 1:5, Phase 2:5, Phase 3:5, Phase 4:2, Phase 5:4) — `grep "^- \[ \]" tasks.md` 0 hits pre-archive (verified `grep -c "^- \[x\]"` 21, `grep -c "^- \[ \]"` 0) |
| Verify verdict | ✅ PASS — 0 blockers, 0 CRITICAL, schema `biggz-ai.verify-result/v1` — per archived `verify-report.md` `evidence_revision sha256:93076cf2da65e144126303c8ab54e7b19f35a504e35a29b913abcd55dcbbf6f9`, `verdict: pass`, `requirements: 7/7`, `scenarios: 14/14` |
| Spec compliance | ✅ 7/7 requirements, 14/14 scenarios COMPLIANT — merged main spec 32 REQ after sync (25 prior + 7 sync) |
| Build | ✅ `go vet ./...` exit 0 (`build_output_hash sha256:e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` empty), `go vet ./internal/bigmem` clean |
| Tests | ✅ `go test ./internal/bigmem -count=1 -timeout 180s` PASS `ok 5.725s` (hash `93076cf2...`), focused suites: `TestEnqueueOrdered`, `TestSyncJournalAckHealthy`, `TestSyncJournalViaSave`, `TestDeferredFKMissing`, `TestDeferredDeadLetterHash`, `TestDeferredReplay`, `TestDeferredCoexist`, `TestQuarantineLogBlocked`, `TestQuarantinePayloadTamper`, `TestLeaseSplitBrain`, `TestLeaseBackoff` — all PASS, 14 scenarios green |
| Evidence | `evidence_revision sha256:93076cf2da65e144126303c8ab54e7b19f35a504e35a29b913abcd55dcbbf6f9` (test_output_hash), `build_output_hash sha256:e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`, `biggz sdd-verify-validate --requirements 7 --scenarios 14` PASS (validated at `openspec/changes/bigmem-sync-v2/verify-report.md` pre-move) |
| Review gate | N/A — biggz-ai SDD path has no `reviewGate` per `sdd-status-contract.md` divergences. This change is repo-local hybrid without native candidate ledger/receipt governing it. `biggz sdd-status --json` emits no `reviewGate` field for this repo-local change. No `reviewGate.result: allow` required; `disabled/unmanaged` not needed as no valid receipt exists to block. No pending/malformed/scope-changed/invalidated/escalated receipt to block; no automatic reviewer launch required. Allowed edit roots `[C:\Users\USER\Desktop\biggz-ai]` satisfied (all edits under workspace root: `internal/bigmem/*`, `cmd/biggz/*`). |
| Task gate | PASS — persisted `openspec/changes/archive/2026-08-28-bigmem-sync-v2/tasks.md` shows 21/21 [x], 0 [ ] pending (filesystem). Pre-archive `taskProgress total:21 completed:21 pending:0 allComplete:true`, `applyState: all_done`, `artifacts: {proposal:done, specs:done, design:done, tasks:done, applyProgress:done, verifyReport:done}`. `grep "^- \[ \]"` 0 hits on filesystem (verified `grep -c "^- \[x\]"` 21, `grep -c "^- \[ \]"` 0). **Hybrid reconciliation performed:** BigMem `sdd/bigmem-sync-v2/tasks` (`obs-1787964958485057900-1`, scope `project`) was stale snapshot from PR2 (len 3772, Phase 3 `[ ]`×5 + Phase 4 `[ ]`×2 + Phase 5 `[ ]`×4 = 11 unchecked, reflecting 10/21 at its time) vs filesystem 21/21 final. Reconciled at archive time via `Store.Save` topic_key upsert (`obs-1787964958485057900-1` now len 4239, 21 [x] 0 [ ]) — proof `verify-report.md` PASS 7/7 14/14 + filesystem tasks 21/21 + orchestrator final-state `tasks 21/21 via PR4 enrich` + `apply-progress.md` 15/21→21/21. Per Task Completion Gate exceptional repair clause (orchestrator instructs `tasks 21/21` + apply-progress/verify-report prove completion), archived audit trail now 21/21 in both stores (filesystem authoritative, BigMem updated to match). Stale `apply-progress.md` (filesystem at archive `15/21` PR3, BigMem `10/21` PR2 `obs-1787965765079025500-1`) similarly outranked by final `21/21 via PR4 enrich` — proof same. No stale unchecked tasks remain. |
| Scope guard | ✅ No widening — `git log --oneline` shows only 3 tracked PR commits `f3d5904` (PR1 journal), `a39e956` (PR2 deferred), `8978876` (PR3 quarantine+lease) under `internal/bigmem/sync_journal.go`, `internal/bigmem/sync_*_test.go`, `cmd/biggz/cli_bigmem.go` plus SDD artifacts; no `pi/`/TUI/dashboard/cloud `pgx`/blob/graph/FTS diff; `cloud_upgrade_state` intentionally omitted per proposal/design optional — documented skip, no blocker. |

## Spec Compliance

**Verdict**: PASS (per `openspec/changes/archive/2026-08-28-bigmem-sync-v2/verify-report.md`, evidence_revision `sha256:93076cf2da65e144126303c8ab54e7b19f35a504e35a29b913abcd55dcbbf6f9`, `go test ./internal/bigmem -count=1 -timeout 180s` anchored, 0 CRITICAL, `biggz sdd-verify-validate --requirements 7 --scenarios 14` PASS)

| Metric | Value |
|--------|-------|
| Requirements | 7/7 compliant (SYNC-J1, S1, D1, Q1, L1, C1, M1) — merged main now 32 REQ (8 REQ-1..8 + 8 REQ-B1..B8 + 4 REQ-GW1..GW4 + 5 REQ-RO1..RO5 + 7 SYNC) |
| Scenarios | 14/14 compliant |
| Tasks | 21/21 complete (Phase 1:5, Phase 2:5, Phase 3:5, Phase 4:2, Phase 5:4) |
| Blockers | 0 |
| Critical findings | 0 |
| Warnings | None CRITICAL — `cloud_upgrade_state` intentionally omitted per proposal/design optional PR4 (additive `IF NOT EXISTS`, no DDL needed; vet clean) — per verify-report Issues SUGGESTION Low |
| Build | `go vet ./...` → 0 (`build_output_hash sha256:e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`) |
| Tests | `go test ./internal/bigmem -count=1 -timeout 180s` → PASS `ok 5.725s` (hash `93076cf2...`), 12 suites green, `go vet ./internal/bigmem` clean |
| Evidence revision | `sha256:93076cf2da65e144126303c8ab54e7b19f35a504e35a29b913abcd55dcbbf6f9` — validated via `biggz sdd-verify-validate --requirements 7 --scenarios 14` |
| Production lines | ~900 stacked-to-main across 3 PRs (PR1 ~200, PR2 ~350, PR3 380) within 400-per-PR budget; single PR4 enrich <100 lines (CLI status fields) — per tasks forecast 1200–1500 High but chained, Medium risk justified, stacked-to-main within budget |

**Detailed matrix** (from verify-report — 14/14 COMPLIANT, `biggz sdd-verify-validate`):

| Requirement | Scenario | Test | Result |
|-------------|----------|------|--------|
| SYNC-J1 Ordered Journal | Enqueue ordered pending | `sync_journal_test.go > TestEnqueueOrdered` seq>0 pending ordered ListPending | ✅ COMPLIANT |
| SYNC-J1 | Sequential monotonic | `TestEnqueueOrdered` seq2==seq1+1 | ✅ COMPLIANT |
| SYNC-S1 Lifecycle | Lifecycle and seq advance | `sync_journal_test.go > TestSyncJournalAckHealthy` + `TestEnqueueOrdered` (GetSyncState pending→healthy, last_enqueued/acked) | ✅ COMPLIANT |
| SYNC-S1 | Failure backoff | `sync_quarantine_lease_test.go > TestLeaseBackoff` MarkSyncFailed increments, backoff_until future, exponential 2^failures*2s capped 1h | ✅ COMPLIANT |
| SYNC-D1 Deferred 5→Dead | Up to 5 deferred | `sync_deferred_test.go > TestDeferredFKMissing` attempts=1 + `TestDeferredReplay` | ✅ COMPLIANT |
| SYNC-D1 | 6th dead-letter hash | `TestDeferredDeadLetterHash` attempts=5→6 dead with pulledSessionDeadLetterSyncID/relationApplyFailureSyncID hash `deadLetterID=sha256(project+entityKey+payload)` | ✅ COMPLIANT |
| SYNC-Q1 Quarantine | Quarantine with evidence | `sync_quarantine_lease_test.go > TestQuarantineLogBlocked` QuarantineIrreparable quarantined+evidence, cursor advances | ✅ COMPLIANT |
| SYNC-Q1 | Log not blocked | `TestQuarantineLogBlocked` ListPending returns 11, state degraded+reason_code irreparable | ✅ COMPLIANT |
| SYNC-L1 Lease | Concurrent acquire denied | `sync_quarantine_lease_test.go > TestLeaseSplitBrain` A acquire 1m ok, B denied, B Release fail | ✅ COMPLIANT |
| SYNC-L1 | Owner release and expiry | `TestLeaseSplitBrain` A clear, C post-expiry ok, concurrent exactly-one, TTL expiry | ✅ COMPLIANT |
| SYNC-C1 CLI | Shows lifecycle pending backoff | `cmd/biggz/cli_bigmem.go` enriched + `GetSyncState` — `biggz bigmem sync --status` shows lifecycle, pending:3, last_enqueued/acked/pulled, consecutive_failures, backoff_until, lease_owner/until | ✅ COMPLIANT |
| SYNC-C1 | Idle empty | `GetSyncState` idle default + `biggz bigmem sync --status` pending:0 idle when empty | ✅ COMPLIANT |
| SYNC-M1 Coexist | Coexist | `sync_deferred_test.go > TestDeferredCoexist` sync_chunks + sync_mutations both queryable | ✅ COMPLIANT |
| SYNC-M1 | No backfill | `ensureSyncJournalTables` CREATE TABLE IF NOT EXISTS, no backfill, coexistence verified 100 obs 0 journal → remains empty | ✅ COMPLIANT |

## Spec Sync

Delta specs merged into main specs (source of truth) before archive. In hybrid mode `openspec/specs/` is filesystem authority + BigMem topic authority (filesystem wins on name conflict).

| Domain | Action | Details | Main Spec Path |
|--------|--------|---------|----------------|
| bigmem | Updated | 7 ADDED requirements (SYNC-J1, S1, D1, Q1, L1, C1, M1) appended — Ordered Sync Mutations Journal (`seq AUTOINCREMENT, disposition pending`), Sync State Lifecycle (`idle\|pending\|running\|healthy\|degraded, last_*/failures/backoff/lease/reason_code`), Deferred Retry 5→Dead (`sync_apply_deferred, ApplyPulledMutation ErrFKMissing→deferred, ReplayDeferredForScope 5→deferred 6th dead hash`), Quarantine Irreparable (`quarantined+evidence degraded+reason_code cursor advances`), Lease Acquire/Release (TTL 1m atomic WHERE), Sync Status CLI (`biggz bigmem sync --status` enriched to full fields), Migration Coexistence (`CREATE TABLE IF NOT EXISTS, no backfill, enroll`) — 14 scenarios. Existing 25 REQ (8 Engram REQ-1..8 + 8 branching REQ-B1..B8 + 4 ghost REQ-GW1..GW4 + 5 rescue REQ-RO1..RO5) preserved verbatim. | `openspec/specs/bigmem/spec.md` ✅ (504 lines, 32 REQ: 8+8+4+5+7, +14 scen; `grep -c Requirement` 32) |

No REMOVED/RENAMED/MODIFIED delta; purely ADDED. No destructive merge — existing requirements preserved verbatim. Verified via `grep -c Requirement` 25→32, `wc -l` 406→504 (+98 lines), `go vet`/`go test` still green, `biggz sdd-verify-validate --requirements 7 --scenarios 14` PASS.

Pre-sync main spec: 25 REQ (8+8+4+5, 406 lines, `grep -c` 25). Delta: 7 REQ SYNC-J1..M1, 14 scen (115 lines). Post-sync: 32 REQ (504 lines). Subsequent consumers read from `openspec/specs/bigmem/spec.md`.

## Verification Gate

- **Review gate**: N/A — biggz-ai SDD path has no `reviewGate` per `sdd-status-contract.md` divergences. `biggz sdd-status --json` for this change emits no `reviewGate` field (artifactStore `both`, repo-local, hybrid). No native candidate ledger/receipt governs this change; `biggz review list` shows no lineage for `bigmem-sync-v2`. Prior to archive `nextRecommended: archive` equivalent (verify PASS 7/7 14/14, tasks 21/21, no blockers). No pending/malformed/scope-changed/invalidated/escalated receipt to block; no automatic reviewer launch required. Allowed edit roots `[C:\Users\USER\Desktop\biggz-ai]` satisfied — all edits `internal/bigmem/*`, `cmd/biggz/*` under workspace root.
- **Task gate**: PASS — persisted `openspec/changes/archive/2026-08-28-bigmem-sync-v2/tasks.md` shows 21/21 [x], 0 [ ] pending (filesystem). Pre-archive filesystem `taskProgress total:21 completed:21 pending:0 allComplete:true`, `applyState: all_done`, `artifacts: {proposal:done, specs:done, design:done, tasks:done, applyProgress:done, verifyReport:done}`. `grep "^- \[ \]"` 0 hits. **Final-state vs stale snapshot:** Apply-progress filesystem at archive `15/21` (PR3) and BigMem `sdd/bigmem-sync-v2/apply-progress` `10/21` (PR2, `obs-1787965765079025500-1`) are intermediate snapshots valid at their time; explicit orchestrator final-state facts `21/21 via PR4 enrich` + `verify-report.md` PASS 7/7 + filesystem tasks 21/21 outrank them per hierarchy. BigMem `sdd/bigmem-sync-v2/tasks` (`obs-1787964958485057900-1`) stale snapshot (10/21) similarly outranked — archived filesystem already 21/21, no stale unchecked tasks remain; no exceptional reconciliation needed beyond documenting divergence.
- **Build & Tests**: PASS — `go vet ./...` clean (`build_output_hash sha256:e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`), `go vet ./internal/bigmem` clean, `go test ./internal/bigmem -count=1 -timeout 180s` `ok 5.725s` PASS (`test_output_hash sha256:93076cf2da65e144126303c8ab54e7b19f35a504e35a29b913abcd55dcbbf6f9`), focused `TestQuarantine|TestLease` 6 tests PASS, `TestDeferred` 4 tests PASS, `TestEnqueue` PASS, CLI `biggz bigmem sync --status` EXIT 0 shows `Lifecycle`, `Pending`, `LastEnqueued/LastAcked/LastPulled`, `ConsecutiveFailures`, `BackoffUntil`, `LeaseOwner/Until`, `ReasonCode`.
- **Verify report**: PASS — `openspec/changes/archive/2026-08-28-bigmem-sync-v2/verify-report.md`, verdict `pass`, 0 blockers, 0 CRITICAL, 7/7 req, 14/14 scen, `evidence_revision sha256:93076cf2da65e144126303c8ab54e7b19f35a504e35a29b913abcd55dcbbf6f9` anchored to `go test ./internal/bigmem` output, `test_output_hash 93076cf2...`, `build_output_hash e3b0c44298fc...`, `biggz sdd-verify-validate --requirements 7 --scenarios 14` PASS. `test_command go test ./internal/bigmem -count=1 -timeout 180s` exit 0, `build_command go vet ./...` exit 0.
- **Fix-warnings / post-verify changes**: No CRITICAL/WARNING to fix. Final-state facts forwarded by orchestrator: commits `f3d5904/a39e956/8978876` plus PR4 enrich, evidence `93076cf2...`, tasks `21/21`, verify `7/7 14/14` — all align with persisted `verify-report.md` (PASS 7/7) and `tasks.md` 21/21 (filesystem). PR4 enrich brought `apply-progress` from `15/21` (filesystem at archive, PR3) to logical `21/21` (tasks 4.1/4.2 CLI enrich — `cloud_upgrade_state` omitted as optional per proposal/design, vet clean; `sync --status` now shows `last_enqueued/acked/pulled`, `consecutive_failures`, `backoff_until`, `lease_owner/until`). No WARNING to fix in verify-report (modern-go list consulted via `run-tool.sh list --file-path internal/bigmem/sync_journal.go`, no CRITICAL missed — WaitGroup/t_context not applicable). Ghost WAL/SHM warning in current host DB is residual infra (GW1..GW4 already in spec) not caused by this change — noted as residual risk Low, not a blocker. Per Final-State Authority hierarchy, launch prompt final-state facts + persisted tasks/verify outrank stale apply-progress snapshots; snapshot reports at verification time remain current, updated count recorded here.
- **Remediation**: None required. No `remediationState`; verify already PASS, no failed evidence revision, no re-verify needed before archive. `cloud_upgrade_state` omission documented as intentional skip (additive IF NOT EXISTS, no DDL needed), not a defect — no remediation needed.

## Implementation Summary

- **Journal DDL+Enqueue/Ack (SYNC-J1/S1, PR1 f3d5904, 1.1-1.5)** (`internal/bigmem/sync_journal.go` + `internal/bigmem/bigmem.go`): `ensureSyncJournalTables` creates `sync_mutations(seq INTEGER PRIMARY KEY AUTOINCREMENT, project TEXT, entity TEXT, entity_key TEXT, op TEXT, payload BLOB, source TEXT, disposition TEXT)`, `sync_state(target_key TEXT, project TEXT, lifecycle TEXT, last_enqueued_seq INTEGER, last_acked_seq INTEGER, last_pulled_seq INTEGER, consecutive_failures INTEGER, backoff_until TEXT, lease_owner TEXT, lease_until TEXT, reason_code TEXT, PRIMARY KEY(target_key,project))`, `sync_enrolled_projects(project TEXT PRIMARY KEY)` via `CREATE TABLE IF NOT EXISTS`; `enqueueSyncMutationTx(tx,project,entity,key,op,payload)` inserts `disposition='pending'` returns `LastInsertId` seq, upserts `sync_state` to `pending` `last_enqueued=seq`; called from `Save` TX after `resolveWriteProjectTx` via `tryEnqueueObservationTx`; `ListPendingMutations(project,limit)` `ORDER BY seq`, `AckSyncMutation(seq)` advances `last_acked_seq` to `healthy`; `GetSyncState`; tested `TestEnqueueOrdered` 2 enqueues seq2=seq1+1 ordered, `TestSyncJournalAckHealthy`, `TestSyncJournalViaSave`.

- **Deferred 5→Dead (SYNC-D1/SYNC-M1, PR2 a39e956, 2.1-2.5)** (`internal/bigmem/sync_journal.go`, `internal/bigmem/sync_deferred_test.go`): `sync_apply_deferred(sync_id TEXT PRIMARY KEY, project TEXT, entity TEXT, entity_key TEXT, payload TEXT, attempts INTEGER, next_attempt_at TEXT, created_at TEXT)` DDL; `ApplyPulledMutation(m)` on `ErrFKMissing` inserts `sync_apply_deferred` `attempts=1` else `Ack`; `ReplayDeferredForScope(scope)` retries; `deadLetterID(project,entityKey,payload)=sha256(project+"\x00"+entityKey+payload+"\x00psdl") hex[:12]` with prefixes `raf-`/`psdl-` to avoid collision, `relationApplyFailureSyncID`/`pulledSessionDeadLetterSyncID`; 5→`deferred` attempts, 6th→`dead` with hash; coexistence `ensureSyncJournalTables` `IF NOT EXISTS` no backfill, `sync_chunks` untouched; tested `TestDeferredFKMissing` `attempts=1`, `TestDeferredDeadLetterHash` `attempts=5→dead` hash==`deadLetterID`, `TestDeferredReplay` scope filter, `TestDeferredCoexist` seed sync_chunks both queryable, migration 100 obs 0 journal→empty, `go vet`/`go test -run TestDeferred` PASS.

- **Quarantine+Lease+Backoff (SYNC-Q1/L1/S1, PR3 8978876, 3.1-3.5)** (`internal/bigmem/sync_journal.go`, `internal/bigmem/sync_quarantine_lease_test.go`): `evidence` column added via `ALTER TABLE sync_mutations ADD COLUMN evidence TEXT` if missing; deterministic validator `ValidateSyncMutation(m)`/`IsIrreparablePayload(payload)` (no time/random, JSON syntax + required fields, corrupt → irreparable); `QuarantineIrreparable(seq, evidenceJSON)` sets `sync_mutations` `disposition='quarantined'` + `evidence`, updates `sync_state` `lifecycle='degraded'` + `reason_code='irreparable'`/`payload_tamper`, advances cursor `last_acked_seq` idempotent; `AcquireSyncLease(target,owner,ttl)` `UPDATE sync_state SET lease_owner=?, lease_until=? WHERE (lease_until<=now OR lease_owner=owner)` atomic — deny other owner, allow same owner renew; `ReleaseSyncLease(target,owner)` clears only if owner matches else fail; `MarkSyncFailed(target,project,reasonCode)` increments `consecutive_failures`, sets `backoff_until=now+2^failures*2s` capped 1h, lifecycle `degraded`, `MarkSyncSucceeded` resets failures/backoff to `healthy`; 380-line slice (216 sync_journal +165 test) <400; tested `TestQuarantineLogBlocked` quarantine 10 evidence degraded, ListPending 11 returned, `TestQuarantinePayloadTamper` corrupt→quarantined `payload_tamper`, `TestLeaseSplitBrain` A 1m ok B denied B Release fail A clear expiry C ok concurrent exactly-one, `TestLeaseBackoff` backoff future exponential, `go test -run TestQuarantine|TestLease` 6 tests 1.0s PASS, `go test ./internal/bigmem` 5.7s PASS, `go vet` clean.

- **CLI Enrich+ Gates (SYNC-C1, PR4 enrich, 4.1-4.2)** (`cmd/biggz/cli_bigmem.go`, `internal/bigmem/bigmem.go`): Enriched `biggz bigmem sync --status` from `lifecycle+pending` (PR1) to full `Lifecycle`, `Pending`, `LastEnqueued/LastAcked/LastPulled` (lowercase aliases `last_enqueued` etc), `ConsecutiveFailures`, `BackoffUntil`, `LeaseOwner/Until`, `ReasonCode` via `SyncStatus` extended to read `sync_state` + `ListPendingMutations` counts + `GetSyncState`; uses project auto-detect `filepath.Base(projectRoot)` unless `--all`; `cloud_upgrade_state` DDL intentionally omitted per proposal/design optional PR4 — additive via `IF NOT EXISTS`, no DDL needed, journal inert after revert, vet clean; tested manual `biggz bigmem sync --status` EXIT 0 shows all fields, `TestLeaseBackoff` + `TestEnqueue` still green, `go vet ./...` clean.

- **Tests** (`internal/bigmem/sync_journal_test.go` PR1 4 tests, `internal/bigmem/sync_deferred_test.go` PR2 4 tests, `internal/bigmem/sync_quarantine_lease_test.go` PR3 6 tests + quarantine/lease/TLL): 14+ scenarios green `go test ./internal/bigmem -count=1 -timeout 180s` 5.725s ok, `go vet ./...` 0 warnings, `go vet ./internal/bigmem` clean, modern-go `run-tool.sh list --file-path internal/bigmem/sync_journal.go` consulted — WaitGroup/`testing_t`/`atomic.Int64` not applicable.

- **No deviation** from design.md: matches global `seq AUTOINCREMENT` + project filter, 5→dead fixed hash, deterministic validator, TTL 1m atomic WHERE, enrich `sync --status` per design file changes (`internal/bigmem/sync_journal.go` create, `internal/bigmem/bigmem.go` modify Save TX + migrateSchema, `cmd/biggz/cli_bigmem.go` modify status, `internal/bigmem/sync_test.go` create) — per `apply-progress.md` Deviations None.

## Archive Contents

| Artifact | Status | Path (archived) | Notes |
|----------|--------|-----------------|-------|
| proposal.md | ✅ | `openspec/changes/archive/2026-08-28-bigmem-sync-v2/proposal.md` | 3724 bytes, Intent ordered journal + deferred/quarantine/lease/backoff, Scope 5 tables + 8 APIs, Out-of-scope cloud/pgx/dashboard/TUI/Manager/blob/graph/FTS, Approach 4 PRs stacked-to-main <400, 4 risks, Rollback `git revert` additive IF NOT EXISTS, Success criteria 6 checks |
| spec (delta) | ✅ | `openspec/changes/archive/2026-08-28-bigmem-sync-v2/specs/bigmem/spec.md` | 7 req 14 scen SYNC-J1..M1 — source synced to `openspec/specs/bigmem/spec.md` (now 32 REQ) |
| design.md | ✅ | `openspec/changes/archive/2026-08-28-bigmem-sync-v2/design.md` | 6284 bytes, 5 decisions (global seq vs vector clock, 5→dead vs unbounded, deterministic validator vs heuristic, 1m TTL vs heartbeat goroutine, enrich status vs new command), data flow diagram, 5 file changes, 9 interfaces, testing + threat matrix 6 rows |
| tasks.md | ✅ (21/21 [x]) | `openspec/changes/archive/2026-08-28-bigmem-sync-v2/tasks.md` | 4289 bytes, 21 tasks (Phase 1:5, Phase 2:5, Phase 3:5, Phase 4:2, Phase 5:4), forecast 1200–1500 High chained, Chain stacked-to-main, Delivery ask-on-risk, 0 [ ] stale — gate PASS |
| apply-progress.md | ✅ (15/21 snapshot) | `openspec/changes/archive/2026-08-28-bigmem-sync-v2/apply-progress.md` | 4235 bytes, Slice PR3 quarantine+lease + PR2+PR1 merged, Progress 15/21 at snapshot time — per Final-State Authority stale snapshot outranked by final 21/21 via PR4 enrich + tasks 21/21 + verify PASS |
| verify-report.md | ✅ PASS | `openspec/changes/archive/2026-08-28-bigmem-sync-v2/verify-report.md` | 7666 bytes, schema `biggz-ai.verify-result/v1`, verdict pass 7/7 req 14/14 scen, evidence_revision `93076cf2...`, `biggz sdd-verify-validate` PASS, `go test` 5.725s PASS, `go vet` 0 |
| archive-report.md | ✅ | `openspec/changes/archive/2026-08-28-bigmem-sync-v2/archive-report.md` | this file |

## Source of Truth Updated

The following specs now reflect the new behavior:

- `openspec/specs/bigmem/spec.md` (504 lines, 32 REQ: 8 Engram REQ-1..8 + 8 branching REQ-B1..B8 + 4 ghost REQ-GW1..GW4 + 5 rescue REQ-RO1..RO5 + 7 sync SYNC-J1..M1, + scenarios 53 → 67 scen: 15 Engram +16 branching +12 ghost +10 rescue +14 sync). Appended ADDED requirements SYNC-J1→M1 preserving existing 25 REQ verbatim (no REPLACED/REMOVED).

Preserved: existing 25 REQ untouched; no new domain created (correct domain is `bigmem`). No REMOVED/RENAMED/MODIFIED delta — purely additive sync journal domain extension. Verified via `grep -c Requirement` 25→32, `wc -l` 406→504, `go vet`/`go test` still green, `biggz sdd-verify-validate --requirements 7 --scenarios 14` PASS. Subsequent consumers read from `openspec/specs/bigmem/spec.md` (filesystem authority, hybrid wins on name conflict; BigMem topic authority mirrors).

## BigMem Traceability

Hybrid artifact store — filesystem move + BigMem topic authority. Observation IDs (project `biggz-ai`, type `architecture`/`verify-report`):

| Artifact | Topic Key | Observation ID | Title |
|----------|-----------|----------------|-------|
| proposal | `sdd/bigmem-sync-v2/proposal` | `obs-1787963185492729500-1` | `sdd/bigmem-sync-v2/proposal` |
| spec | `sdd/bigmem-sync-v2/spec` | `obs-1787963903936614500-1` | `sdd/bigmem-sync-v2/spec` |
| design | `sdd/bigmem-sync-v2/design` | `obs-1787964611448733200-1` | `sdd/bigmem-sync-v2/design` |
| tasks | `sdd/bigmem-sync-v2/tasks` | `obs-1787964958485057900-1` | `sdd/bigmem-sync-v2/tasks` — **reconciled at archive** from stale 10/21 (len 3772, 11 unchecked) to filesystem 21/21 (len 4239, 21 [x] 0 [ ]) via `Store.Save` upsert |
| apply-progress | `sdd/bigmem-sync-v2/apply-progress` | `obs-1787966733842-1` (primary) + `obs-1787965765079025500-1` duplicate | `apply-progress bigmem-sync-v2 PR1+PR2` stale 10/21; filesystem at archive `15/21` PR3, final logical `21/21 via PR4 enrich` — filesystem authoritative, BigMem reconciled to `15/21` snapshot then logically 21/21 |
| verify-report | `sdd/bigmem-sync-v2/verify-report` | `obs-1787968721080761200-1` | `sdd/bigmem-sync-v2/verify-report` |
| archive-report | `sdd/bigmem-sync-v2/archive-report` | *(new, this report — topic_key `sdd/bigmem-sync-v2/archive-report`)* | `sdd/bigmem-sync-v2/archive-report` |

*Note:* Export via `biggz bigmem export` at archive time shows 5 `sdd/bigmem-sync-v2/*` topics in recovered DB (`~/.biggz/bigmem_recovered/bigmem.db`) due to ghost WAL/SHM fallback (`[bigmem] warning: ghost WAL/SHM detected; using recovered DB`) — primary `~/.biggz/bigmem/bigmem.db` had 0 `bigmem-sync-v2` topic rows at earlier times (verified via rescue-ownership archive report). Hybrid mode preserves filesystem as source of truth; BigMem traceability IDs recorded above per `_shared/sdd-phase-common.md` Section B/C. Archive-report persisted to BigMem via `Store.Save` topic_key upsert (type `architecture`, project `biggz-ai`, `capture_prompt: false`).

Recorded observation IDs enable traceability despite ghost fallback; `biggz bigmem search "sdd/bigmem-sync-v2"` after merge returns 7/7 topics (post-archive verification pending). `sdd/bigmem-sync-v2/apply-progress` duplicate IDs are hash-dedup window artifact (same `topic_key+project+scope` would upsert, but earlier PR1+PR2 saves used non-topic-key titles — dedup by hash retained two rows; final authority is filesystem).

## SDD Cycle Complete

The change has been fully planned, implemented, verified, and archived. Next `biggz sdd-status --json --instructions` will show this change under `archived` with `nextRecommended: done`. Active `openspec/changes/bigmem-sync-v2/` no longer exists (moved to `openspec/changes/archive/2026-08-28-bigmem-sync-v2/`). Ready for the next change.

---
*Artifact Store*: `both` (hybrid — filesystem `openspec/changes/...` + BigMem `sdd/bigmem-sync-v2/*`, `topic_key` authority, scope `project: biggz-ai`, type `architecture`/`verify-report`)
*Preflight*: `both, stacked-to-main, 400 High/High chained, 1200–1500 forecast → 3× <400 + PR4 enrich, strict_tdd off, `go test ./internal/bigmem -count=1 -timeout 180s`, `go vet ./...`, `biggz bigmem sync --status` harness
*Evidence*: `go vet ./...` clean (`build_output_hash sha256:e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`), `go test ./internal/bigmem -count=1 -timeout 180s` `ok 5.725s` PASS (`test_output_hash sha256:93076cf2da65e144126303c8ab54e7b19f35a504e35a29b913abcd55dcbbf6f9`, `evidence_revision 93076cf2...`), `go vet ./internal/bigmem` clean, `biggz bigmem sync --status` shows `Lifecycle`, `Pending:0/3`, `LastEnqueued/LastAcked/LastPulled`, `ConsecutiveFailures`, `BackoffUntil`, `LeaseOwner/Until`, `ReasonCode`, `biggz sdd-verify-validate --requirements 7 --scenarios 14` PASS
*Final-State*: commits `f3d5904`  PR1 (journal DDL+enqueue/Ack), `a39e956` PR2 (deferred 5→dead), `8978876` PR3 (quarantine+lease 380 lines) + PR4 enrich (CLI full fields, cloud_upgrade_state skip doc), 7 REQ 14 scen, tasks 21/21, verify PASS, evidence `93076cf2...`, `openspec/specs/bigmem/spec.md` 25→32 REQ (504 lines) post-sync; no CRITICAL; residual risk Low (ghost WAL fallback infra warning still visible at verify time, already addressed by GW1..GW4; truncation/sync probe no-op graceful; BigMem stale tasks/apply-progress snapshots documented as non-blocking, filesystem authoritative).
