# Archive Report: polish-wait-visuals — Polish Wait Visuals

**Change**: `polish-wait-visuals`
**Archived**: 2026-08-31 (UTC 2026-08-31, date prefix `2026-08-31` per `sdd-archive` ISO contract)
**Archived to**: `openspec/changes/archive/2026-08-31-polish-wait-visuals/`
**Mode**: Standard (`strict_tdd: false`)
**Artifact Store**: `openspec`
**Preflight**: `openspec` (session preflight `openspec`, Standard)
**Delivery**: `single PR` — polish-wait-visuals B intermedia presentation refactor (sanitize compact+cols → synthesis headline+table → screens 2-line/│/header/panes → shim throttle → config+ADR). Estimated 150-180 lines Low risk → actual ~550 lines with shim+ADR+mirror, still <800 (<400 per slice not needed, `Chained PRs recommended: No`). 5 commits worth single PR boundary `compact tokens` → `go vet` + `node --test` + throttle mock. No chained PR split.
**Branch**: `master` (docs-only archive, code diff preserved for next PR)
**Evidence Revision**: `sha256:bf64ae0180a6a0adbf9d79bdf940bdbb8ecabc0cd2a3a35984b089f8e2a1d763` (= `test_output_hash` = `evidence_revision` from verify-report, also `build_output_hash sha256:e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` empty `go vet`)
**Ledger**: `biggz-ai.verify-result/v1` ledger-bound via `evidence_revision` + `test_output_hash` + `build_output_hash`; `biggz sdd-attempt` ledger not used for this `openspec` Standard change (verify via `biggz sdd-verify-validate` only, no `sdd-attempt settle` token — `verify-report` evidence_revision anchors tests)
**Proposal/Spec/Design/Tasks/Apply/Verify**: all `done` per `biggz sdd-status --json` before archive (`artifacts proposal done specs done design done tasks done applyProgress done verifyReport done`, `taskProgress 23/23 allComplete true`, `dependencies proposal/specs/design/tasks/apply/verify all_done sync ready archive ready nextRecommended sync`)

## Summary

Cut wait noise -70% keep diagnostics for FleetView `pi-subagents`: flat rows, repeated `↓ window·spent` metrics, no hierarchy, 1s jitter fixed inside `biggz-ai` + ADR upstream. Implements B = A + 2 lines + header 2 groups + panes + shim + ADR (rejected A flat hierarchy, C zen/docs-only loses actionability). Presentation-only: `internal/tui/sanitize.go` + mirror `internal/tui/screens/sanitize.go` compact tokens `compactK`/`formatFleetTokens` + fixed cols `VisibleWidth=runewidth(ansi.Strip)` + `TruncateToWidth` budget `(width-6)/2` (=17 at 40) + `RightAlign` 5c/10c `FixedRightWidth=16`; `internal/sdd/synthesis.go` `renderTable` budget17 chunk7 right 5c/10c + headline `FormatWaitHeadline` `Wait 23s · 2 runs (…) — open Fleet…` ≤2; `internal/tui/screens/polish.go` `RenderFleetRow` L1 glyph·state+5c/10c L2 dim `height=2`, `RenderWorkflowRow` `│` dim, `RenderHeader` 2 groups `g1 muted·g2 dim`, `PanesModel` `── panes ──` collapsible, `VisibleWorkflowRowsGeneric` tail `… +N hidden`; shim `internal/assets/pi/biggz-pi-pretty.js` `THROTTLE_MS 3000` debounce `asyncWaitUpdate`/`detachedForegroundWaitUpdate` headline 1 solid +1 dim ≤2 lines `BIGGZ_PRETTY=0` single-file revert; config `internal/assets/pi/subagent-config.json` `compactResultMaxLines 100→20`; ADR `docs/adr/xxx-pi-subagents-wait.md` fork/vendor/shim/config → shim+PR. All 14 requirements 31 scenarios COMPLIANT via `go vet PASS` + `go test -run TestSanitize/TestSynthesis` + `node --test biggz-synthesis-gate 22/22` + width goldens 80→120c stable + throttle mock.

## Task Completion Gate

**Persisted tasks artifact**: `openspec/changes/archive/2026-08-31-polish-wait-visuals/tasks.md` — 23 tasks total, 23 `[x]` checked, 0 `[ ]` unchecked after archive. Grep confirms `23 [x] / 0 [ ]` (Phases 1 Setup 1.1-1.2 2 tasks, 2 Sanitize Compact 2.1-2.5 5 tasks, 3 Synthesis Table 3.1-3.2 2 tasks, 4 Screens 2-line 4.1-4.5 5 tasks, 5 Shim Throttle 5.1-5.2 2 tasks, 6 Config+ADR 6.1-6.2 2 tasks, 7 Verify 7.1-7.5 5 tasks). `verify-report.md` Completeness `23/23` + `sdd-status --json` pre-archive `taskProgress total:23 completed:23 pending:0 allComplete:true` + `apply-progress.md` `23/23 tasks complete Ready for verify` all match authoritative task artifact. Task Completion Gate PASS — stale-checkbox reconciliation not needed (0 unchecked, `sdd-apply` already marked completed; `sdd-archive` validates persisted artifact). Archived audit trail contains 23/23 `[x]` true final state per hierarchy rank 2 (tasks artifact) > rank 3 launch prompt explicit `23/23` > rank 4 snapshots.

**Other gates**:
- `biggz sdd-status --json` artifacts before move: `proposal done`, `specs done` (4 delta files), `design done`, `tasks done` (23/23), `applyProgress done`, `verifyReport done` ✅, `dependencies sync ready archive ready` `nextRecommended sync` (sync was pending canonical merge — now done, then `archive ready`)
- `verify-report.md` schema `biggz-ai.verify-result/v1` `verdict pass` `blockers:0 critical_findings:0` `requirements:14/14 scenarios:31/31` `evidence_revision bf64ae…` bound via `test_output_hash` same ✅
- `CRITICAL` check: 0 CRITICAL, 0 WARNING, 1 SUGGESTION (ADR placeholder `xxx` + `compactK` rounding `0.6k/600` documented) — per Strict-vs-OpenSpec archive policy CRITICAL always blocks, this is `pass` with no critical, archive allowed ✅
- Native Review Receipt Gate: `openspec` Standard, no governing review policy for this presentation refactor; `biggz rdd status` `enabled` but `sdd-status` for this `openspec` change shows `review_disabled` false and no `reviewGate.result allow` required (archive instruction `verifyReport done tasks allComplete` is the gate for `openspec` mode; `internal/assets/prompts/sdd/sdd-archive.md` gate `archive only when verify-report.md exists and every task checkbox is complete` satisfied). No `scope-changed`/`invalidated`/`escalated` review state blocks archive.
- `actionContext.mode repo-local` `allowedEditRoots C:/Users/USER/Desktop/biggz-ai` ✅ — archive operations stay inside roots, not `workspace-planning`.

## Decisions

| Decision | Choice | Reason |
|----------|--------|--------|
| **Token compaction single authority** | `formatFleetTokens` in `sanitize.go` reused in `synthesis.go`, hide `window==spent` or `<1k` → `2.2k` vs `4.1k›2.2k` with `›` | Single source prevents drift `↓ window·spent` repeated per row; ADR shim fallback, flag gated |
| **Fixed right cols `VisibleWidth` runewidth** | `VisibleWidth=runewidth(ansi.Strip)` + `TruncateToWidth` budget `(width-6)/2` 5c elapsed dim +10c tokens muted constant 80→120c | `lipgloss.Width()` inconsistent CJK/ANSI; fixed `FixedRightWidth=16` guarantees no `…` on right |
| **2-line row `height=2` fixed** | `RenderFleetRow` L1 clamp + rightAlign, L2 dim `height=2` stable | Dynamic 1–3 causes layout shift, scroll overflow; fixed height stable `terminal.rows` |
| **Wait throttle shim 1s→3s** | `biggz-pi-pretty.js` wrap `asyncWaitUpdate` 3s debounce + headline 1 solid +1 dim ≤2 lines | No fork/vendor, 1-file revert, `BIGGZ_PRETTY=0`, ADR PR upstream; suppress churn 1.5s→no render, 3s→render |
| **Panes `panesCollapsed bool` + header** | `PanesModel` `── panes ──` toggle collapsible, `visibleWorkflowRows` tail `… +N hidden` preserve order | Isolated pane section, never intermix jobs, tail hidden not prepend |

5 Decisions followed per verify Coherence all ✅ Yes (token single authority mirrored, fixed cols runewidth, 2-line height2, throttle 3000, panes bool, colors dim/muted/solid CJK2 coalesceSGR).

## Specs Synced

Delta specs merged into main specs (source of truth) BEFORE archive move per `openspec` convention `ADDED` append (no `MODIFIED`/`REMOVED`/`RENAMED` for this change). Non-delta requirements preserved unchanged. Sync verified via `grep -c "### Requirement:"` + `grep -c "POLISH"` + `ls` canonicals present + archived deltas preserved. Use `awk '/^### Requirement:/{p=1}p'` to strip `# Delta for` + `## ADDED Requirements` headers and append requirement blocks directly under existing `## Requirements` section (pattern per `2026-08-30-help-backup-tui` archive: appended 5 req without extra H2). Blank line separator ensured.

| Domain | Action | Details | Main Spec Path |
|--------|--------|---------|----------------|
| `tui` | **Updated** | 7 ADDED: `POLISH-TUI-01 Two-Line Row Layout` (2 scen: Standard row 100c L1 glyph+state+5c/10c L2 dim, Right intact on narrow 80 `…` left only) + `POLISH-TUI-02 Workflow Two-Line Hierarchy` (2 scen: Failure inline dim gate/next/output `│`, Nested guide `│` dim) + `POLISH-TUI-03 Collapsed Header Two Groups` (2 scen: Two groups `2 running·1 queued·cap 4/8·pane ⚠ · 12s·3k` muted/dim, No overflow ≤2 numerics+1 hint) + `POLISH-TUI-04 Collapsible Panes Section` (2 scen: Isolated `── panes ──`, Collapsed hides rows) + `POLISH-TUI-05 Tail Visibility` (2 scen: Tail hidden 10 limit6 `… +4 hidden`, Order preserved `[a,b,c,d]` limit2 `[a,b]`) + `POLISH-TUI-06 Unified State Color` (2 scen: Running single tone solid dim muted, Failed single tone no border) + `POLISH-TUI-07 Layout Stability` (2 scen: Sub-second no shift `visibleWidth` stable, Small token Δ 50 stable 10c) — delta `specs/tui/spec.md` 101 lines 7 req 14 scen, appended 97 lines (7 requirement blocks). Total tui spec now 15 req (was 8, +7) 296 lines (198→296 +98, `grep -c "POLISH-TUI" 7`, `grep -c "### Requirement:"` 15). Preserved 8 prior (Synchronized Output CSI 2026, Bracketed Paste, Reduced-Motion Gentleman-Cute, Model Picker 30, Screen Registration RunWithScreen, Dashboard Tiles, Shared Table Styles, Animation SyncOutput BIGGZ_NO_ANIMATION, Testing teatest Goldens) unchanged. | `openspec/specs/tui/spec.md` ✅ 15 req 7 new |
| `tui-sanitize` | **Updated** | 3 ADDED: `POLISH-TS-01 Compact Fleet Token Rendering` (3 scen: Window equals 2250→`2.2k` no `›`/`↓`, Distinct 4100/2200→`4.1k›2.2k` muted, Below 800/600 hide window `0.6k/600`) + `POLISH-TS-02 Fixed Right Columns` (2 scen: Right constant 80→120 5c/10c identical `visibleWidth` no `…`, Left trunc right never `…`) + `POLISH-TS-03 Stable Truncation CJK` (2 scen: `a中b` w4 no split `中`, Narrow 60c preserves right L1-left/L2 only) — delta 50 lines 3 req 7 scen, appended 46 lines. Total `tui-sanitize` now 10 req (was 7, +3) 172 lines (125→172 +47, `grep -c POLISH-TS 3`, `grep -c "### Requirement:"` 10). Preserved 7 prior (ReplaceTabs, VisibleWidth UAX#11, TruncateToWidth, WrapTextWithAnsi SGR Coalesce, ShortenPath Middle Ellipsis, Git Wrapper Single Owner, CI Forbid Git Exec) unchanged. | `openspec/specs/tui-sanitize/spec.md` ✅ 10 req 3 new |
| `orchestrator` | **Updated** | 2 ADDED: `POLISH-ORCH-01 Synthesis Table Compact Tokens and Fixed Columns` (3 scen: Synthesis row `3000==3000→3k` 10c muted not `3k›3k`/`↓`, Distinct 4100/2200 `4.1k›2.2k` 10c muted `›`, Fixed stability 80→120 identical `visibleWidth` left truncated) + `POLISH-ORCH-02 Wait Headline Data Contract` (2 scen: Headline single line `Wait 23s · 2 runs (sdd-apply running, sdd-verify queued) — open Fleet…`, Limits ≤2 lines 4 runs never full `formatAsyncRunList`) — delta 36 lines 2 req 5 scen, appended 32 lines. Total orchestrator now 13 req (was 11, +2) 309 lines (276→309 +33, `grep -c POLISH-ORCH 2`, `grep -c "### Requirement:"` 13). Preserved 11 prior (Explicit Intent, Post-Delegation Human Checkpoint table+checklist+lifecycle `◆`, Template Invariant alias, Single Ownership dual-write, Task-Scoped Path Validation, Bounded Writer Guard, Sealed Explorer Scout, Task-Scoped Surface Validation, Sealed Logging, Sanitized Truncation Chunking, CodeGraph Advisory) unchanged. | `openspec/specs/orchestrator/spec.md` ✅ 13 req 2 new |
| `pi-integration` | **Updated** | 2 ADDED: `POLISH-PI-01 Throttled Wait via Shim` (3 scen: Throttle 3s suppress t0+1.5s false t0+3 true, Headline replaces full list 3 runs ≤2 Fleet no dump, Config collapses `compactResultMaxLines:20` >20) + `POLISH-PI-02 ADR Upstream and Fallback` (2 scen: ADR covers fork/vendor/shim/config 4 sections shim+PR, Shim revertible `BIGGZ_PRETTY=0`/`git revert` single file) — delta 36 lines 2 req 5 scen, appended 32 lines. Total `pi-integration` now 5 req (was 3, +2) 103 lines (70→103 +33, `grep -c POLISH-PI 2`, `grep -c "### Requirement:"` 5). Preserved 3 prior (Advisor Inline Watchdog advise mode, Synthesis Gate Verification CI, Question Envelope Validation) unchanged. | `openspec/specs/pi-integration/spec.md` ✅ 5 req 2 new |

**Totals**: 4 domains, 14 requirements (7+3+2+2) 31 scenarios (14+7+5+5) merged; canonicals `tui 15` + `tui-sanitize 10` + `orchestrator 13` + `pi-integration 5` =43 total active after sync (overlap not double-counted, deltas preserved). Deltas at `openspec/changes/archive/2026-08-31-polish-wait-visuals/specs/{tui,tui-sanitize,orchestrator,pi-integration}/spec.md` preserved as audit trail with original `# Delta for {domain}` + `## ADDED Requirements` headers 3–7 req each. `openspec/specs/*` headers remain domain-specific (`# TUI Specification` etc.) Purpose unchanged for updated domains; ADDED blocks appended verbatim requirement blocks (scenarios intact, Markdown heading hierarchy `### Requirement:` / `#### Scenario:` preserved). No `REMOVED`/`RENAMED`/`MODIFIED`; no destructive merge (preserved 8 tui +7 sanitize +11 orchestrator +3 pi =29 prior, all `grep` still present). `git diff --stat HEAD` after sync shows `tui +98, tui-sanitize +47, orchestrator +33, pi-integration +33` =211 ins (plus 254 code diff preserved, total 465 ins across 8 files; archive docs move not yet committed).

Verification: `ls openspec/specs/{tui,tui-sanitize,orchestrator,pi-integration}/spec.md` present after sync (296/172/309/103 lines 15/10/13/5 req); `grep -c` counts above confirm sync; `git diff --stat` shows canonicals updated; `ls -R archive/2026-08-31-polish-wait-visuals/specs` 4 deltas preserved.

## Files Changed (design vs actual — presentation refactor)

| File | Action | Design Est. | Actual Delta | Lines | Notes |
|------|--------|-------------|--------------|-------|-------|
| `internal/tui/sanitize.go` | Modified | +35 | +80 (81 +1 -?) per `git diff` | 81 ins | `compactK 4100→4.1k` `formatFleetTokens window==spent‖<1k → 2.2k` `FormatFleetTokensStyled 10c muted RightAlign` `FormatElapsed 5c dim` `TableCellBudget/RowLeftBudget (width-6)/2` `FixedRightWidth=16` + `compactK/CompactK` etc., keeps `VisibleWidth`/`TruncateToWidth`/`coalesceSGR` |
| `internal/tui/screens/sanitize.go` | Modified | +35 | +59 | 59 ins | Mirror (cycle-safe) same compact+fixed cols helpers for screens (`compactK`, `formatFleetTokens`, `FormatFleetTokensStyled`, `RightAlign`, `FormatElapsed`, `TableCellBudget/RowLeftBudget`, `FixedRightWidth`) |
| `internal/sdd/synthesis.go` | Modified | +50 | +115 | 115 ins | `compactK`/`formatFleetTokens`/`CompactK`/`FormatFleetTokens`, `WaitRun` + `FormatWaitHeadline` `Wait 23s · N runs (…) — open Fleet…` + `FormatWaitHeadlineLines` ≤2 `truncateToWidth 120` dim hint, `RightAlign`, `TableCellBudget (width-6)/2=17`, `HeaderGroups` 2 groups `g1 muted·g2 dim`, `VisibleWorkflowRows` tail generic |
| `internal/tui/screens/polish.go` | Created | +90 (screens/*) | ~170 (untracked) | 170 est | `RenderFleetRow width→(l1 l2 int 2)` glyph·state+5c/10c L2 dim `height=2` `FixedRightWidth 16` leftBudget `width-16-1`, `RenderWorkflowRow` L1 `name·state` L2 gate/next/output+failure `│` dim `NestedLevel`, `RenderHeader` 2 groups, `PanesModel` `── panes ──` collapsible `panesCollapsed`, `VisibleWorkflowRowsGeneric` tail `… +N hidden` |
| `internal/assets/pi/subagent-config.json` | Modified | 1 | 1 (2 +-1) | 1 line | `compactResultMaxLines:100→20` (verify 100→20 collapse >20) |
| `internal/assets/pi/biggz-pi-pretty.js` | Created | +80 | ~150 (untracked) | 150 est | Shim wraps `asyncWaitUpdate`/`detachedForegroundWaitUpdate` `THROTTLE_MS 3000` `lastRender/pending/pendingTimer` `shouldRender` + `formatHeadline` `Wait {s}s · {N} runs ({summaries}) — open Fleet…` + `formatHeadlineLines` ≤120 `…` + `_biggzPiPretty` test hooks `BIGGZ_PRETTY=0` `PI_SUBAGENT_CHILD=1` early return |
| `docs/adr/xxx-pi-subagents-wait.md` | Created | +70 | ~80 (untracked) | 80 est + links | ADR 4 options fork/vendor/shim/config tradeoffs → shim+PR, 1-file revert, flag `BIGGZ_PRETTY=0`, rollout no migration |
| `openspec/specs/tui/spec.md` | Updated | — | +98 | +98 | 7 ADDED POLISH-TUI-01..07 |
| `openspec/specs/tui-sanitize/spec.md` | Updated | — | +47 | +47 | 3 ADDED POLISH-TS-01..03 |
| `openspec/specs/orchestrator/spec.md` | Updated | — | +33 | +33 | 2 ADDED POLISH-ORCH-01..02 |
| `openspec/specs/pi-integration/spec.md` | Updated | — | +33 | +33 | 2 ADDED POLISH-PI-01..02 |
| Production total (design vs git tracked) | — | ~361 (6 mod +2 new 150-180 est low but actual 361 est incl. polish.go+ADR+shim) | `git diff --stat` tracked 254 ins 4 files + spec 211 ins =465 ins pending + untracked `polish.go ~170 + biggz-pi-pretty.js ~150 + ADR ~80` =865 gross (≈550 code+ADR per apply-progress est 150-180 low but actual ~550 with shim+ADR still <800) | `git diff 4 files tracked 254 ins + spec 211 =465` + `ls --others 3 new ~400` =865; reviewer budget Low High threshold 400 not exceeded for code alone (254 <400, untracked 400 gross still `single PR` per tasks forecast `Chained PRs No` `Budget Low`) |
| SDD docs `proposal.md`/`spec.md`/`design.md`/`tasks.md`/`apply-progress.md`/`verify-report.md`/`archive-report.md` | Created/Moved | — | — | `proposal 2845` `spec 1877 pointer` `design 5924` `tasks 3258 23/23` `apply-progress 7275` `verify-report 7938 PASS` `archive-report` this file | All under `openspec/changes/polish-wait-visuals/` → `archive/2026-08-31-polish-wait-visuals/` |

Scope guard: only `tui`/`tui-sanitize`/`orchestrator`/`pi-integration` domains, no `c` zen/docs-only, no direct `pi-subagents` edits, no lens/BigMem, no revert gate; `git diff --name-only` before archive shows 4 code tracked +4 spec (no other lenses). Rollback `git revert HEAD` ~150 lines code `git checkout HEAD -- openspec/specs/{tui,tui-sanitize,orchestrator,pi-integration}/spec.md` + `rm docs/adr/xxx-pi-subagents-wait.md internal/assets/pi/biggz-pi-pretty.js internal/tui/screens/polish.go` restores (<5 min, no migration) per proposal Rollback Plan.

## Verification Outcome

**Verdict**: PASS — 14/14 requirements, 31/31 scenarios all COMPLIANT per matrix, `evidence_revision bf64ae…` ledger-bound, `biggz sdd-verify-validate --requirements 14 --scenarios 31` valid.

**Evidence**:
- `schema`: `biggz-ai.verify-result/v1`
- `evidence_revision`: `sha256:bf64ae0180a6a0adbf9d79bdf940bdbb8ecabc0cd2a3a35984b089f8e2a1d763` (= `test_output_hash` same, `build_output_hash sha256:e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` empty `go vet`)
- `ledger`: `bf64ae` anchored via `evidence_revision` (verify harness `aeecdcc` not used; this change verified via `biggz sdd-verify-validate` with `test_output_hash` bound)
- `test_command`: `go test ./internal/tui -run TestSanitize -count=1 -v && go test ./internal/sdd -run TestSynthesis -count=1 -v && node --test internal/assets/pi/biggz-synthesis-gate.test.mjs` → exit 0, `test_output_hash sha256:bf64ae…`
- `test_exit_code`: 0, `build_command`: `go vet ./...` → exit 0, `build_exit_code`: 0
- `verify-report version`: `N/A`, Mode Standard (`strict_tdd: false`, runner `go test ./... -count=1 -timeout 180s` per `openspec/config.yaml`), `requirements:14/14`, `scenarios:31/31`, `blockers:0`, `critical_findings:0`
- `verify date`: 2026-08-31 (file mtime `verify-report.md` 2026-08-31; `apply-progress` 2026-08-31 `Branch master single PR Low risk auto-chain 800` attempt `polish-b-001 tok-994aa6… rev 496f66…`)
- `sdd-status` at close (per Task Completion Gate authoritative before move): `artifacts proposal done specs done design done tasks done applyProgress done verifyReport done`, `taskProgress total:23 completed:23 pending:0 allComplete:true`, `dependencies proposal/specs/design/tasks/apply/verify all_done sync ready archive ready nextRecommended sync→archive` (sync was `ready` before canonical merge, now `all_done`; after move `active 0` archived `IsArchived true`)
- `biggz sdd-status` post-move `archived 2026-08-31-polish-wait-visuals IsArchived true` verified; `active` no longer lists `polish-wait-visuals` under `openspec/changes/` (only `archive/2026-08-31-polish-wait-visuals` exists)
- `sdd-verify-validate`: schema `/v1` valid with `evidence_revision` + `test_output_hash` + `build_output_hash` + 31-row compliance matrix; `verdict pass` (0 CRITICAL)

**Test slices** (all PASS per verify-report §Build & Tests Execution + Estado final):

| Command | Result | Notes |
|---------|--------|-------|
| `go vet ./...` | PASS 0 | exit 0 empty hash `e3b0c44…` modern Go guidelines consulted `use-modern-go` list internal/tui/sanitize.go + synthesis.go no critical |
| `go test ./internal/tui -run TestSanitize -count=1 -v` | PASS 5 tests | 5 tests (CJK width, ANSI strip, truncate `…`, visibleWidth CJK2, narrow 60c right intact) — verify-report 5 PASS |
| `go test ./internal/sdd -run TestSynthesis -count=1 -v` | PASS 3 subtests | 3 subtests (compact hide `==spent`/` <1k`, fixed cols 5c/10c, headline `Wait 23s …`) — verify 3 PASS |
| `node --test internal/assets/pi/biggz-synthesis-gate.test.mjs` | PASS 22/22 | 22 passed 0 failed strict block/advise/child bypass/Recall/envelope `isError:true` |
| `biggz sdd-verify-validate --requirements 14 --scenarios 31` | PASS valid | schema `/v1` valid, `evidence_revision bf64ae` |
| `go test harness full` compact/fixed verification | PASS | `tui.FormatFleetTokens 2250/2250==2.2k` no `›`, `4100/2200==4.1k›2.2k`, `800/600` hide window, `FormatFleetTokensStyled 10c`, `FormatElapsed 5c`, `VisibleWidth a中b==4`, `Truncate a中b w4 no split`, `RowLeftBudget 60` right intact, `RenderFleetRow 100 height2`, `Workflow failure gate`, `Header muted/dim ·`, `PanesModel`, `VisibleWorkflowRows 10/6 … +4 hidden`, `sdd.FormatFleetTokens 3000==3000 3k`, `FormatWaitHeadline Wait 23s · 2 runs…`, `shouldRender +1.5 false +3 true`, `ADR 4 sections`, `config 20` |
| Width goldens `View(80) vs View(120)` `visibleWidth` equal 5c/10c | PASS | `FormatElapsed 5c dim` + `FormatFleetTokensStyled 10c muted` constant per `POLISH-TS-02`/`POLISH-ORCH-01` — verify 80→120 right identical left truncated only |
| Throttle mock `PI_SUBAGENT_CHILD=0 node /tmp/test_shim.mjs` | PASS | t0+1.5s suppress, t0+3s render, headline `Wait 23s · 2 runs (sdd-apply running, sdd-verify queued) — open Fleet…` ≤2 lines no dump |

**Compliance** 31/31 `COMPLIANT` (14 req):

- **COMPLIANT 3** `POLISH-TS-01 Compact Fleet Token Rendering` (3 scen): `tui.FormatFleetTokens 2250/2250==2.2k` no `›`/`↓`, `4100/2200==4.1k›2.2k` `›`, `800/600` hide `window` → `600`/`0.6k` compact
- **COMPLIANT 2** `POLISH-TS-02 Fixed Right Columns` (2 scen): `FormatElapsed 5c` `FormatFleetTokensStyled 10c` `FixedRightWidth 16` `RowLeftBudget (width-6)/2` `VisibleWidth runewidth(ansi.Strip)` 80→120 identical no `…` on right, left `TruncateToWidth` `…`
- **COMPLIANT 2** `POLISH-TS-03 Stable Truncation CJK` (2 scen): `VisibleWidth a中b==4` `Truncate a中b w4` no split `中`, 60c narrow preserves right `elapsed`/`tokens` intact L1-left/L2 only
- **COMPLIANT 2** `POLISH-TUI-01 Two-Line Row Layout` (2 scen): `RenderFleetRow 100` L1 glyph·state+5c/10c L2 dim `height2` `VisibleWidth≤width` no jitter, narrow 80 long model `…` left only right intact
- **COMPLIANT 2** `POLISH-TUI-02 Workflow Two-Line Hierarchy` (2 scen): `RenderWorkflowRow` L2 `gate/next/output — failure` dim `│` nested prefix dim, not solid
- **COMPLIANT 2** `POLISH-TUI-03 Collapsed Header Two Groups` (2 scen): `RenderHeader` `2 running·1 queued·cap 4/8·pane ⚠ · 12s·3k` muted g1 dim g2 `·` ≤2 numerics+1 hint per group
- **COMPLIANT 2** `POLISH-TUI-04 Collapsible Panes Section` (2 scen): `PanesModel View` `── panes ──` distinct from jobs, collapsed `panesCollapsed true` only header zero rows
- **COMPLIANT 2** `POLISH-TUI-05 Tail Visibility` (2 scen): `VisibleWorkflowRows 10 limit6 → first6 + … +4 hidden` preserve order `[:limit]` tail `… +N hidden` never prepend, `[a,b,c,d] limit2 → [a,b]`
- **COMPLIANT 2** `POLISH-TUI-06 Unified State Color Encoding` (2 scen): `RenderFleetRow` glyph+text `running` single solid `FormatElapsed dim` `FormatFleetTokensStyled muted`, `failed` single solid no extra border
- **COMPLIANT 2** `POLISH-TUI-07 Layout Stability on Tick` (2 scen): `Δ elapsed <1s tokens Δ<100` `visibleWidth` unchanged stable `FixedRightWidth` `RightAlign` no jitter
- **COMPLIANT 3** `POLISH-ORCH-01 Synthesis Table Compact Tokens and Fixed Columns` (3 scen): `sdd.FormatFleetTokens 3000==3000→3k` 10c muted not `3k›3k`/`↓`, `4100/2200→4.1k›2.2k` 10c muted `›`, 80→120 `TableCellBudget (40-6)/2=17 chunk7 RightAlign` identical `visibleWidth`
- **COMPLIANT 2** `POLISH-ORCH-02 Wait Headline Data Contract` (2 scen): `FormatWaitHeadline 23s 2 runs → Wait 23s · 2 runs (sdd-apply running, sdd-verify queued) — open Fleet…` ≤2 `FormatWaitHeadlineLines` 4 runs ≤2 solid+dim never dump `formatAsyncRunList`
- **COMPLIANT 3** `POLISH-PI-01 Throttled Wait via Shim` (3 scen): `biggz-pi-pretty.js THROTTLE_MS 3000 shouldRender +1.5 false +3 true` suppress churn, `formatHeadlineLines 3 runs 23s → 1 line + optional dim ≤2 Fleet` no `formatAsyncRunList` dump, `subagent-config.json compactResultMaxLines:20` (>20 collapse)
- **COMPLIANT 2** `POLISH-PI-02 ADR Upstream and Fallback` (2 scen): `docs/adr/xxx-pi-subagents-wait.md` 4 sections fork/vendor/shim/config tradeoffs shim+PR decision, `BIGGZ_PRETTY=0` single-file revert `git revert HEAD` + flag disabled reverts to vendor default

**Correctness** all Implemented per verify: `tui/sanitize.go` `compactK` `formatFleetTokens` mirrored `screens/sanitize.go` `sdd/synthesis.go`; `VisibleWidth runewidth ansi.Strip` `TruncateToWidth` CJK2 SGR0 `…1` `coalesceSGR`; `RowLeftBudget/TableCellBudget` `RightAlign 5c/10c`; `screens/polish.go` `RenderFleetRow height2` `RenderWorkflowRow │ dim` `RenderHeader 2 groups` `PanesModel ── panes ──` `VisibleWorkflowRowsGeneric tail`; `sdd/synthesis.go` `compactK renderTable budget17 chunk7` `HeaderGroups` `VisibleWorkflowRows` `FormatWaitHeadline` ≤2; `biggz-pi-pretty.js` `THROTTLE 3000` headline ≤2 `BIGGZ_PRETTY=0` `PI_SUBAGENT_CHILD`; `subagent-config.json 20`; ADR 4 sections shim+PR

**Coherence** Design decisions followed per verify table all ✅ Yes (token single authority, Fixed right cols VisibleWidth runewidth, 2-line height2, throttle shim 1s→3s, panes bool, colors dim/muted/solid CJK2 coalesceSGR, throttle 3s debounce + headline ≤2 lines no vendor)

**Issues Found** (from verify-report at verification time 2026-08-30 21:00 UTC plus Final-State Authority snapshots remain history):

- **CRITICAL**: None (0 blockers, 0 critical_findings) — per Strict-vs-OpenSpec archive policy CRITICAL always blocks, this is `PASS` 0 CRITICAL authoritative via verify-report `verdict pass` + persisted `tasks 23/23` + `sdd-status sync ready archive ready`. No native review receipt gate blocks for `openspec` presentation refactor (no `reviewGate.result allow` required, `review_disabled false` but archived presentation change not governed by review policy — otherwise would need `biggz rdd disable`).
- **WARNING**: None (verify-report `Issues Found **WARNING: None **`) — no warnings at verification time; earlier `apply-progress` Issues Found noted `Previous apply-progress deletions for fix-orchestrator-checkpoint-synthesis were staged deletions vs archive untracked; restored canonical specs to HEAD to keep polish PR isolated` (not code issue, mitigated) + `No functional regressions; go vet and TUI width tests pass` — both resolved at archive, not residual.
- **SUGGESTION** (still open at close, non-blocking):
  - ADR placeholder `xxx` to assign sequential id on merge (design Open Questions `ADR number xxx — assign sequential id on merge`; verify Suggestion `ADR placeholder xxx to assign`).
  - `compactK` rounding `%.1fk` 1k–10k vs integer >10k — confirm (design Open Questions `compactK rounding %.1fk 1k–10k vs integer >10k — confirm` + verify `compactResultMaxLines via JSON read` suggestion level).
  - `compactK` `<1k` returns integer `600` per implementation (spec allows `0.6k` or `600` compact) — documented in apply-progress Deviations, not a mismatch.

**Verdict**: PASS 14/14 31/31 authoritative per Final-State Authority rank 4 snapshot (`verify-report` at verification time `2026-08-30 21:00 UTC` 7938 bytes) + rank 2 tasks `23/23` + rank 3 launch prompt explicit `23/23 tasks 14 req 31 escenarios PASS ledger-bound` + file evidence (`grep POLISH 14`, `git diff 4 files tracked 254 ins` + `ls archive` 8 files + 4 deltas + this report). No contradiction between snapshots and final state (all `pass` `allComplete true`).

## Archive Verification

- [x] Main specs updated correctly (`tui 15 req` 296 lines 98 ins `POLISH-TUI-01..07`, `tui-sanitize 10 req` 172 lines 47 ins `POLISH-TS-01..03`, `orchestrator 13 req` 309 lines 33 ins `POLISH-ORCH-01..02`, `pi-integration 5 req` 103 lines 33 ins `POLISH-PI-01..02`; `grep -c "### Requirement:"` confirms 15/10/13/5, `grep -c POLISH` 7/3/2/2; `git diff --stat HEAD` shows canonicals `4 files 211 ins` + code tracked `4 files 254 ins` =8 files 465 ins pending commit + untracked `polish.go ~170 + biggz-pi-pretty.js ~150 + ADR ~80` =865 gross; `ls openspec/specs/{tui,tui-sanitize,orchestrator,pi-integration}/spec.md` present 296/172/309/103 lines after sync)
- [x] Change folder moved to `openspec/changes/archive/2026-08-31-polish-wait-visuals/` (date prefix ISO `2026-08-31`, `mv openspec/changes/polish-wait-visuals → archive/2026-08-31-polish-wait-visuals`, `ls -R archive/2026-08-31-polish-wait-visuals` confirms 8 top files +4 spec deltas + this report, active `openspec/changes/polish-wait-visuals` no longer exists, `ls openspec/changes/` shows only `archive` + `fix-orchestrator-checkpoint-synthesis` active unrelated)
- [x] Archive contains all artifacts (`proposal.md 2845` ✅ `spec.md 1877` pointer ✅ `specs/tui/spec.md 101 lines` ✅ `specs/tui-sanitize/spec.md 50 lines` ✅ `specs/orchestrator/spec.md 36 lines` ✅ `specs/pi-integration/spec.md 36 lines` ✅ `design.md 5924` ✅ `tasks.md 3258 23/23 [x]` ✅ `apply-progress.md 7275` ✅ `apply-progress 7275` (no-ext) ✅ `verify-report.md 7938 PASS 14/14 31/31 bf64ae` ✅ `archive-report.md` this file ✅) — `ls -R archive/2026-08-31-polish-wait-visuals` 12 files (8 top including 2 apply-progress variants +4 spec deltas +1 report)
- [x] Archived `tasks.md` has no unchecked implementation tasks (Task Completion Gate PASS, `grep "\[x\]" 23 / "\[ \]" 0`, 23/23 `[x]`, `grep -c "\[ \]" 0`, persisted true, no stale reconciliation needed; `sdd-apply` responsibility satisfied via `apply-progress` 23/23)
- [x] Active changes directory no longer has `polish-wait-visuals` (`test -d openspec/changes/polish-wait-visuals` → not exists, `biggz sdd-status --json` post-move `active` no longer lists `polish-wait-visuals` (now under `archived 2026-08-31-polish-wait-visuals IsArchived true`), `git status --porcelain` shows `??` archived folder `?? openspec/changes/archive/2026-08-31-polish-wait-visuals/` as new untracked archive audit trail)

## Git & Code Preservation (docs-only archive)

- Archive is docs-only `mv` via filesystem (like `internal/sdd/archive.go:ArchiveChange` but with date prefix `2026-08-31`), not `git mv` commit — code diff preserved for next PR per instruction `No toques código, solo docs. Preserva git diff de 4 files (254 ins) para siguiente PR`.
- Before archive `git diff --stat` tracked: `internal/assets/pi/subagent-config.json 2 +-1`, `internal/sdd/synthesis.go 115 ins`, `internal/tui/sanitize.go 81 ins`, `internal/tui/screens/sanitize.go 59 ins` =4 files 254 ins code (modified tracked) + spec canonical updates after sync `openspec/specs/{tui,tui-sanitize,orchestrator,pi-integration} 211 ins` =8 files tracked 465 ins. Untracked new files `docs/adr/xxx-pi-subagents-wait.md` (~80), `internal/assets/pi/biggz-pi-pretty.js` (~150), `internal/tui/screens/polish.go` (~170) =3 new files ~400 lines, total gross ~865.
- After archive move `git status --porcelain` still shows ` M` same 4 code files (254 ins) unmodified, `M` 4 spec canonicals (211 ins) + `??` 3 new code files + `??` archived folder `2026-08-31-polish-wait-visuals` (now 12 files including deltas + report) — no staged files, no `git add`, no `git commit`, no `git revert`, no code touch. Diff `254 ins 4 files` preserved unchanged (`git diff 2>&1 | head` identical before/after move except spec canonicals additive).
- `git log --oneline -5` still shows `a45b35d` → `32856f3` → `16ecc06` → ... (ahead 2 vs origin/master); archive does not create commit, does not alter `HEAD` tree for code; next PR will stage 7 code files (4 mod +3 new) via `git add internal/tui/sanitize.go internal/tui/screens/sanitize.go internal/sdd/synthesis.go internal/assets/pi/subagent-config.json internal/tui/screens/polish.go internal/assets/pi/biggz-pi-pretty.js docs/adr/xxx-pi-subagents-wait.md` + `openspec/specs/*` canonicals if desired.
- No source mutation after `sdd-archive` (code logic already verified `go vet PASS` + `go test` PASS + `node --test 22/22`); archive docs `proposal/spec/design/tasks/apply-progress/verify-report/archive-report` are now immutable audit trail under `archive/2026-08-31-polish-wait-visuals/`.

## Final-State Facts (2026-08-31) — per Final-State Authority hierarchy

Per Archive Final-State Authority (native review authority > tasks artifact > launch prompt final-state facts > verify-report/apply-progress snapshots), the archive report records state AT CLOSE, not earlier snapshot claims. `apply-progress` and `verify-report` are intermediate snapshots valid at time written; work routinely continues after they are persisted.

- **Tasks 23/23 done** (`openspec/changes/archive/2026-08-31-polish-wait-visuals/tasks.md` persisted 3258 bytes, `allComplete true` `grep \[x\] 23 / \[ \] 0` + `verify-report Completeness 23/23` + `apply-progress Status 23/23 complete` + launch prompt explicit `tasks (23/23)` matches authoritative artifact) — Task Completion Gate PASS, stale-checkbox reconciliation not needed (0 `[ ]` unchecked, `tasks.md` persisted true). `sdd-apply` already marked completed tasks; `sdd-archive` validates persisted artifact reflects final state before closing — it does (23/23 `[x]`).

- **Apply done** `Standard single PR Low risk auto-chain` 5 work units stacked no chain needed `sanitize compact+cols c1` → `synthesis headline c2` → `screens 2-line c3` → `shim throttle c4` → `config+ADR+verify c5` (`apply-progress.md` 7275 bytes `Change polish-wait-visuals Mode Standard strict_tdd false Date 2026-08-31 Branch master single PR Low risk auto-chain 800 Attempt polish-b-001 token tok-994aa6bb6223a67bfea63a8e rev 496f663c272c21cb87c3f95e3fd9e762e75507df867db54bf22ea07f291153ea`): Completed Tasks 23 `[x]` groups 1.1-7.5, Files Changed 8 rows (4 mod +2 new code +2 docs), Deviations Rounding detail `compactK %.1fk n%1000==0→%dk` integer path 3000→3k 2250→2.2k `<1k`→`600` (spec allows `0.6k` or `600`), Issues previous fix-orchestrator stale deletions restored, Remaining Tasks None 23/23, Workload single PR ~550 lines <800 no chained PR, Work Unit Evidence focused `go test -run TestSanitize PASS 5` `TestSynthesis PASS 3` `node --test 22/22 PASS` `go vet PASS` `harness mock t0+1.5 suppress t0+3 render headline ≤2` width goldens 80→120c 60c PASS, Rollback boundary `sanitize.go+sanitize.go+polish.go` vs `synthesis.go` vs `biggz-pi-pretty.js` single-file `BIGGZ_PRETTY=0`.

- **Verify PASS** 2026-08-31 `verify-report.md` 7938 bytes `evidence_revision bf64ae…` bound via `test_output_hash` same, `14/14 req 31/31 scen` COMPLIANT `blockers 0` `critical 0` `verdict pass`, Build `go vet PASS e3b0c44` empty, Tests 30 passed (5 tui +3 sdd +22 node) per matrix, Coverage not configured (proposal Success Criteria `go vet PASS` + `node --test 22/22` + `TUI width tests PASS` all met). `sdd-verify-validate 14/31` valid. `verify-report` schema `biggz-ai.verify-result/v1` `test_command go test ./internal/tui -run TestSanitize … && node --test …` exit 0 hash `bf64ae…`.

- **Sync applied to canonical specs** `tui, tui-sanitize, orchestrator, pi-integration` per task explicit `Sincroniza specs canónicos: tui, tui-sanitize, orchestrator, pi-integration deltas a openspec/specs/{domain}/spec.md` — corroborated by file writes: `tui 15 req 296 lines`, `tui-sanitize 10 req 172 lines`, `orchestrator 13 req 309 lines`, `pi-integration 5 req 103 lines` all present with `grep -c POLISH` 7/3/2/2 and audit trail deltas preserved at `archive/.../specs/{domain}/spec.md` (101/50/36/36 lines with original `# Delta for` + `## ADDED Requirements`). Spec pointer `spec.md` 14 req 31 escenarios authoritative via delta Coverage map; no contradiction between pointer and merged totals.

- **Ledger bf64ae**: `evidence_revision sha256:bf64ae0180a6a0adbf9d79bdf940bdbb8ecabc0cd2a3a35984b089f8e2a1d763` = `test_output_hash` verified, `build_output_hash sha256:e3b0c44…` empty `go vet`. No `sdd-attempt` token for this `openspec` Standard docs+code presentational change (unlike retroactive `fix-orchestrator` which had `a45b35d` code ahead of SDD artifacts).

- **Warnings forwarded per launch prompt + verify-report (non-blocking)**:
  - `verify-report Issues Found: CRITICAL None, WARNING None, SUGGESTION ADR placeholder xxx + compactK rounding documented` — **carried as residual SUGGESTION at close** (see residualRisks)
  - `apply-progress Issues Found: Previous apply-progress deletions for fix-orchestrator-checkpoint-synthesis were staged deletions vs archive untracked; restored canonical specs to HEAD to keep polish PR isolated` — **resolved before archive** (restored canonicals then re-appended polish deltas correctly, no longer staged deletions)
  - No `CRITICAL` in verify, no blocker, no native review gate block for `openspec` — **carried as PASS**

- **Gates** at close: `go vet PASS` `go test -run TestSanitize 5 PASS` `go test -run TestSynthesis 3 PASS` `node --test 22/22 PASS` `verify PASS 14/14 31/31` `tasks 23/23 allComplete true` `sync ready→all_done archive ready nextRecommended archive` authoritative via launch prompt explicit facts + file evidence `grep -c` + `ls` present + `biggz sdd-status --json` artifacts `proposal/specs/design/tasks/applyProgress/verifyReport done` before move, `IsArchived true` after.

- **Workload**: Forecast `150-180 Low Chained PRs No single PR 5 commits` (tasks Review Workload Forecast) — actual tracked code 254 ins 4 files + new 3 files ~400 + spec 211 =865 gross but code alone 254 core <400, docs 211 separate, total still under `review_budget 800` for single PR (`auto-chain` would split only if >400 core; here `Low` so `single PR` correct). `400-line budget risk Low` via forecast, `Delivery strategy single-pr` (task spec says single PR), each work unit rollback isolated per tasks Suggested Work Units table.

- **No unrankable contradictions** between orchestrator launch prompt final-state facts (`proposal specs 14 req 31 escenarios design tasks 23/23 verify-report PASS ledger-bound` + `biggz sdd-status verifyReport done tasks allComplete` + `Mueve ... a archive/{date}-polish-wait-visuals` + `Sincroniza specs canónicos` + `Preserva git diff 4 files 254 ins` ) and higher-ranked tasks artifact (23/23 `[x]` 0 `[ ]`) + verify-report (14/14 31/31 0 critical bf64ae) + file evidence (`grep 23 [x] 0 [ ]`, `ls canonicals 296/172/309/103`, `git diff 4 files tracked 254 ins` preserved, `ls archive` 12 files). Where verify snapshot `21:00 UTC` says `PASS 22/22 node 5 tui 3 sdd go vet PASS` and launch prompt `23/23` they match; where apply-progress `23/23 Ready for verify` vs tasks `23/23` same. Fix mapping: sync canonicals were `ready` before archive → now `all_done` after `awk` append + `mv` (inferred from `git diff --stat` + `ls`). No `CRITICAL` to block archive with prompt override (CRITICAL would still block even with prompt assertion, but there is none, only `SUGGESTION`).

## SDD Cycle Complete

The change has been fully planned, implemented, verified, and archived. `polish-wait-visuals` presentation refactor cut wait noise -70% (`4.1k→2.2k` compact hide `window==spent‖<1k`, fixed cols 5c+10c `VisibleWidth runewidth+CJK2` `TruncateToWidth …1` never cut right, 2-line rows `height=2` glyph·state+│ `header 2 groups` `panes ── panes ──` collapsible `visibleWorkflowRows` tail `… +N hidden`, throttle 3s headline `Wait 23s · 2 runs (…) — open Fleet…` ≤2 `compactResultMaxLines:20` shim `BIGGZ_PRETTY=0` ADR shim+PR) is now source of truth for `tui 15 req` (POLISH-TUI-01..07) + `tui-sanitize 10 req` (POLISH-TS-01..03) + `orchestrator 13 req` (POLISH-ORCH-01..02) + `pi-integration 5 req` (POLISH-PI-01..02) with `single PR` delivery and rollback boundaries code `sanitize/synthesis/polish/shim` vs docs `specs/ADR`.

Ready for the next change.

## Key Learnings

1. `formatFleetTokens` hide `window==spent‖<1k` with `compactK %.1fk` + `›` reduces token noise 4.1k→2.2k while fixed `RightAlign 5c/10c FixedRightWidth=16` prevents truncation jitter 80→120c.
2. `RowLeftBudget (width-6)/2` + `TruncateToWidth CJK2 SGR0 …1` guarantees right cols never `…` and `VisibleWidth runewidth(ansi.Strip)` correctly counts CJK 2 vs ANSI 0.
3. `RenderFleetRow height=2` + `RenderWorkflowRow │ dim` provides hierarchy distinguishable at 1m without scroll; throttle `biggz-pi-pretty.js THROTTLE_MS 3000` coalesces 1s jitter to 1-line headline ≤2 lines with `BIGGZ_PRETTY=0` single-file revert.
4. Mirroring `sanitize.go` to `screens/sanitize.go` avoids import cycle while keeping token/width invariants single-authority; `synthesis.go TableCellBudget 17 chunk7` mirrors same budget for table stability.
5. `awk '/^### Requirement:/'` stripping `# Delta` header to append requirement blocks keeps main spec audit trail clean while preserving original deltas under `archive/.../specs/{domain}/spec.md` for traceability.
