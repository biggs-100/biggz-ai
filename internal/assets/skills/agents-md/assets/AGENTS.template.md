# <PROJECT> — Agent Skills Index

When working on this project, load the relevant skill(s) BEFORE writing any code.

1. Find your task in the trigger column
2. Read the SKILL.md at the listed path
3. Follow ALL patterns and rules from the loaded skill

<!-- biggz-compat: keep this block so biggz-ai routing keeps working.
     - SDD phase skills (sdd-*) are delegate_only: never invoke them directly;
       the orchestrator delegates. Route new SDD work through sdd-new.
     - Ceremony is quiet by default: one-line reports; full synthesis plus
       checkpoint only before irreversible actions (merge, PR, delete, release).
     - Memory work is invisible: learnings go into saves, never into replies.
     - Register every project skill in the table below with trigger and exact
       path, so skill-registry and sdd-init scans keep resolving. -->

## Skills

| Skill | Trigger | Path |
|-------|---------|------|
| `<skill-name>` | <when to load it> | [`<path/to/SKILL.md>`](<path/to/SKILL.md>) |
