# /skill-creator

Create or update an OpenCode skill using the bundled skill-creator workflow.

Loads the `skill-creator` skill first, then uses it to create or update a skill from the user's request.

If the request is ambiguous, ask one focused clarification before editing files.

**Usage**: `/skill-creator <description of skill to create>`

**Output**: New or updated SKILL.md in the skills directory
