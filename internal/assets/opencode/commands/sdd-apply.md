# /sdd-apply

Apply current SDD tasks — implement pending changes.

Reads the tasks artifact and implements the next pending task(s). Marks tasks complete as they are done.

**Usage**: `/sdd-apply [task-ids...]`

**Output**: Code changes with apply-progress report
