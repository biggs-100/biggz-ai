# /sdd-init

Initialize SDD context for the current project.

Creates `openspec/` directory structure and config, detects testing capabilities, and sets up the skill registry.

**Usage**: `/sdd-init`

**Output**: Initialized SDD workspace at `openspec/config.yaml`
