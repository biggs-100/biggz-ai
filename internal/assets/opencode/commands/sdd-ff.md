# /sdd-ff

Fast-forward through SDD phases for well-understood changes.

Skips directly to implementation. User must acknowledge skipping review phases.

**Usage**: `/sdd-ff <change-name>: <description>`

**Output**: Spec, design, tasks auto-generated → apply phase
