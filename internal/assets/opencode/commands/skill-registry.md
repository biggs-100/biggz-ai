# /skill-registry

Rebuild the skill registry for the current project and installed skills.

Loads the `skill-registry` skill first, then rebuilds `.atl/skill-registry.md` for the current project and configured skill directories.

**Usage**: `/skill-registry`

**Output**: Registry path, skill count, cache status, and whether BigMem was updated
